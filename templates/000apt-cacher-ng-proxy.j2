// {{ ansible_managed }}

// This configuration snipped is intended to be stored in /etc/apt/apt.conf.d/
// on the client system in order to change a regular setup to use apt-cacher-ng.

{% if apt-cacher-ng_cache_host is defined and apt-cacher-ng_cache_host %}
Acquire::http::Proxy "http://{{ apt-cacher-ng_cache_host }}:{{ apt-cacher-ng_port | default('3142') }}/";
{% endif %}


